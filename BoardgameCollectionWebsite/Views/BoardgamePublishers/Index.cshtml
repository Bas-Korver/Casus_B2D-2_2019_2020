@model IEnumerable<BoardgameCollectionWebsite.Models.BoardgamePublisher>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BoardGame.Thumbnail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Publisher.PublisherName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BoardGame.Thumbnail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Publisher.PublisherName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BoardgamePublisherID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BoardgamePublisherID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BoardgamePublisherID })
        </td>
    </tr>
}

</table>
