@model IEnumerable<BoardgameCollectionWebsite.Models.Integration>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IntegrationID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IntegratedBy)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IntegrationID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IntegratedBy)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LocalIntegrationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LocalIntegrationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LocalIntegrationID })
        </td>
    </tr>
}

</table>
