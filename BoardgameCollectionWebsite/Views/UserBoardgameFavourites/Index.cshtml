@model IEnumerable<BoardgameCollectionWebsite.Models.UserBoardgameFavourite>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUser.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BoardGame.Thumbnail)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUser.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BoardGame.Thumbnail)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserBoardgameFavouriteID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserBoardgameFavouriteID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserBoardgameFavouriteID })
        </td>
    </tr>
}

</table>
