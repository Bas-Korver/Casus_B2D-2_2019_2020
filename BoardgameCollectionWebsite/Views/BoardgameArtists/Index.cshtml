@model IEnumerable<BoardgameCollectionWebsite.Models.BoardgameArtist>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artist.ArtistName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BoardGame.Thumbnail)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artist.ArtistName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BoardGame.Thumbnail)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BoardgameArtistID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BoardgameArtistID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BoardgameArtistID })
        </td>
    </tr>
}

</table>
