@model IEnumerable<BoardgameCollectionWebsite.Models.BoardgameDesigner>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BoardGame.Thumbnail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Designer.DesignerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BoardGame.Thumbnail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Designer.DesignerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BoardgameDesignerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BoardgameDesignerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BoardgameDesignerID })
        </td>
    </tr>
}

</table>
