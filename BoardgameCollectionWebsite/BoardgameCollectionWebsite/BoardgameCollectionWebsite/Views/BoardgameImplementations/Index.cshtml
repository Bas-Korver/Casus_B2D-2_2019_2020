@model IEnumerable<BoardgameCollectionWebsite.Models.BoardgameImplementation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BoardGame.Thumbnail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Implementation.ImplementedBy)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BoardGame.Thumbnail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Implementation.ImplementedBy)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BoardgameImplementationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BoardgameImplementationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BoardgameImplementationID })
        </td>
    </tr>
}

</table>
