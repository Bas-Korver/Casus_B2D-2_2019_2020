@model IEnumerable<BoardgameCollectionWebsite.Models.BoardgameIntegration>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BoardGame.Thumbnail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Integration.IntegratedBy)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BoardGame.Thumbnail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Integration.IntegratedBy)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BoardgameIntegrationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BoardgameIntegrationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BoardgameIntegrationID })
        </td>
    </tr>
}

</table>
