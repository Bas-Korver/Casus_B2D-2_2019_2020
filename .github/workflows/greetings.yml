name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Je eerste issue bericht, Nice!'' first issue'
        pr-message: 'Je eerste pull request, Nice!'' first pr'
